procs:
  client-run:
    cwd: client
    shell: yarn vite
    autostart: false
  client-preview:
    cwd: client
    shell: yarn vite preview
    autostart: false
  db:
    shell: docker compose up
    autostart: false
